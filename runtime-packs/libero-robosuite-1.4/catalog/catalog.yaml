schema_version: 1
catalog_version: 0.4.0
entries:
  - scene_id: libero-spatial-0
    name: LIBERO Spatial · Task 0
    description: 固定 suite/task，init state 为只读变体
    tags: [libero, benchmark, franka]
    engine: mujoco
    loader: libero
    source: libero
    compatible_runtime_profile: libero-robosuite-1.4
    versions:
      - version: 1.0.0
        runtime_scene_key: libero_spatial:0
        published: true
        robot_models: [franka_panda]
        variants:
          - {variant_id: init-state-0, name: Init State 0, kind: init_state}
          - {variant_id: libero-pro-environment, name: LIBERO-Pro Environment, kind: evaluation_config}
        capabilities: [viewer, rgb, depth, contact, evaluation]
        authoring: {mode: none}
        evaluation:
          provider: libero
          evaluation_kind: task_success
          metrics: [success, reward]
          supports_comparison: true
          available_variants: [init-state-0, libero-pro-environment]
