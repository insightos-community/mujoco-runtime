# 外部资产仓根目录，插件不会复制场景或 mesh。
MUJOCO_ASSET_ROOT=/opt/semantic/mujoco_asset

# egl 为默认无头后端；无 GPU 主机安装 OSMesa 后可改为 osmesa。
MUJOCO_GL=egl

PLUGIN_MUJOCO_HOST=127.0.0.1
PLUGIN_MUJOCO_PORT=8090
PLUGIN_MUJOCO_BACKEND=mujoco
PLUGIN_MUJOCO_REALTIME=1
